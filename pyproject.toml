[project]
name = "aklia"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv==1.2.3",
    "langgraph==1.2.11",
    "langchain-core==1.6.3",
    "langchain-groq==1.1.3",
    "groq==0.37.1",
]

[tool.vercel]
entrypoint = "api.analyze:app"
